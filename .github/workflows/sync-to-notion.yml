name: Sync to Notion

on:
  push:
  issues:
    types:
      - opened
      - edited
      - closed
      - reopened
      - assigned
      - unassigned
      - labeled
      - unlabeled

jobs:
  sync-to-notion:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync placeholder
        run: |
          echo "Sync to Notion workflow triggered"
          echo "Event: ${{ github.event_name }}"